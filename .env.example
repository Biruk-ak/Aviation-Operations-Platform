# Environment variables (example)

# Copy to backend/.env or project root .env — never commit real secrets.

PORT=4000
AOP_ALLOW_ANON=1

# Example placeholders only
# DATABASE_URL=
# JWT_SECRET=
# WEATHER_API_KEY=
# MAPS_API_KEY=
